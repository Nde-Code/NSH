{
  "name": "NSH Codespaces setup script",
  "image": "mcr.microsoft.com/devcontainers/base:ubuntu",

  "features": {
    "ghcr.io/devcontainers/features/node:1": { "version": "20" },
    "ghcr.io/devcontainers/features/python:1": { "version": "3.10" }
  },

  "postCreateCommand": "python3 -m pip install --upgrade pip requests && npm install -g wrangler && echo \"FIREBASE_HOST_LINK=$FIREBASE_HOST_LINK\" > .dev.vars && echo \"FIREBASE_HIDDEN_PATH=$FIREBASE_HIDDEN_PATH\" >> .dev.vars && echo \"HASH_KEY=$HASH_KEY\" >> .dev.vars && echo \"ADMIN_KEY=$ADMIN_KEY\" >> .dev.vars && wrangler types",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python"
      ],
      "settings": {
        "python.defaultInterpreterPath": "python3"
      }
    }
  },

  "remoteUser": "vscode"
}
